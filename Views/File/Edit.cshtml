@model MvcMovie.Models.LibDocument
@{
  ViewData["Title"] = "Редагувати документ";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}


<h3>Редагування документу</h3>


    <div class="modal-content">
      <div class="modal-header">

        </button>
      </div>
      <div class="modal-body">


        @using(Html.BeginForm("Edit", "File", FormMethod.Post, new { enctype = "multipart/form-data" }))
        {

          @Html.ValidationSummary(true)
       <div class="form-group">

          @Html.LabelFor(model=>model.Name)
          @Html.EditorFor(model=>model.Name,new { htmlAttributes = new { @class = "form-control" } })
          @Html.ValidationMessageFor(model => model.Name)
        <br>
         <div class="input-group mb-3">
          <div class="input-group-prepend">
           
          </div>

          <!-- <div class="custom-file">
            <input type="file" accept="application/pdf,video/*" name="uploadedFile" class="custom-file-input" id="uploadedFile" aria-describedby="inputGroupFileAddon01">
            <label class="custom-file-label" for="inputGroupFile01">Виберіть файл</label>
            
          </div>
           -->
        </div>


        <div class="form-group">
            <label for="sel1">Оберіть категорію:</label>
            @Html.DropDownListFor(model=>model.CategoryId,ViewBag.Categories as SelectList, new{@class="form-control"})
        </div>      
       
        </div>
              @Html.LabelFor(model => model.Desc1)
           @Html.TextAreaFor(model=>model.Desc1,new { htmlAttributes = new { @class = "form-control" } })
            <br>
             
           @Html.CheckBoxFor(model=>model.Published,new { htmlAttributes = new { @class = "form-check-input" } })
           @Html.LabelFor(model => model.Published)
            <br>
            
          @Html.CheckBoxFor(model=>model.AccessLinkOnly,new { htmlAttributes = new { @class = "form-check-input" } })
           @Html.LabelFor(model => model.AccessLinkOnly)
           <br>
        <button type="submit" class="btn btn-primary">Зберегти</button>
        }
      </div>
      <div class="modal-footer">
        <!-- <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        <button type="button" class="btn btn-primary">Save changes</button> -->
      </div>
    </div>
 

