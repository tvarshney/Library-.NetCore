@model MvcMovie.Models.Category
@{
  ViewData["Title"] = "Редагувати категорію";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}


<h3>Редагування категорії</h3>


    <div class="modal-content">
      <div class="modal-header">

        </button>
      </div>
      <div class="modal-body">


        @using(Html.BeginForm("EditCategory", "Category", FormMethod.Post, new { enctype = "multipart/form-data" }))
        {

          @Html.ValidationSummary(true)
       <div class="form-group">

          @Html.LabelFor(model=>model.Name)
          @Html.EditorFor(model=>model.Name,new { htmlAttributes = new { @class = "form-control" } })
          @Html.ValidationMessageFor(model => model.Name)
        <br>
         
        <div class="form-group">
            <label for="sel1">Оберіть батьківську категорію:</label>
            @Html.DropDownListFor(model=>model.parent_id,ViewBag.Categories as SelectList, new{@class="form-control"})
        </div>

        </div>
            @Html.LabelFor(model => model.Desc1)
           @Html.TextAreaFor(model=>model.Desc1,new { htmlAttributes = new { @class = "form-control" } })
            <br>

        <button type="submit" class="btn btn-primary">Зберегти</button>
        }
      </div>
      <div class="modal-footer">
        <!-- <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        <button type="button" class="btn btn-primary">Save changes</button> -->
      </div>
    </div>
 

